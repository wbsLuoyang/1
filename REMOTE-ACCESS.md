# 在外面也能访问 —— Tailscale 组网方案

## 为什么选这个

你的电脑和手机各自加入同一个虚拟内网，手机在外地用 4G/5G 也能直接连回家里电脑的 `8899` 端口。

- **不需要公网 IP**，不需要路由器做端口映射
- **不需要把服务暴露到公网**，密钥始终留在你自己电脑上
- 流量端到端加密，免费版个人用足够（100 台设备）

---

## 一、电脑端

### 1. 安装

下载地址：https://tailscale.com/download/windows

或者用命令行装（需要管理员权限的 PowerShell）：

```powershell
winget install --id tailscale.tailscale -e
```

### 2. 登录

装完后托盘区会出现 Tailscale 图标，点它登录。
可以用 Google / Microsoft / GitHub 账号直接登，不用单独注册。

登录后图标变成实心，说明已连上。

### 3. 拿到这台电脑的 Tailscale IP

打开 PowerShell 或 CMD：

```powershell
tailscale ip -4
```

会输出一个 `100.x.x.x` 的地址 —— **记住它，这就是你在外面访问要用的地址**。

顺手验证一下连接质量：

```powershell
tailscale status
```

---

## 二、手机端

1. App Store / 各大安卓应用商店搜索 **Tailscale**，安装
2. 用**同一个账号**登录
3. 打开开关，状态显示 Connected

---

## 三、访问

手机浏览器打开：

```
http://100.x.x.x:8899      ← 换成第 3 步拿到的那个 IP
```

输入访问密码即可。建议在手机上「添加到主屏幕」，用起来跟 App 一样。

---

## 四、让服务开机自启（不然重启电脑就断了）

1. 在 `ai-hub` 目录下双击 `install-autostart.bat`（会创建一个无窗口启动脚本）
2. 按 `Win + R`，输入 `shell:startup` 回车，打开启动文件夹
3. 把目录里生成的 `start-hidden.vbs` 的**快捷方式**拖进去

这样每次开机登录后服务会自动在后台跑，没有黑窗口。

> 不想自启就跳过这步，每次手动双击 `start.bat` 也一样。

---

## 五、必须知道的局限

| 情况 | 结果 |
|---|---|
| 电脑关机 | ❌ 连不上 |
| 电脑睡眠/休眠 | ❌ 连不上 |
| 电脑开着但断网 | ❌ 连不上 |

**这是所有"服务跑在自己电脑上"方案的通病。** 想真正做到随时可用，只有两条路：

- 让电脑一直开着（电源设置里把"睡眠"设为"从不"）
- 或者换成云平台托管（见 README 的「部署到公网」章节）

---

## 六、常见问题

**手机连不上，但电脑自己访问正常**

按顺序排查：
1. 电脑上 `tailscale status` 看两台设备是否都显示在线
2. 手机上 Tailscale 开关是否打开
3. 用 `tailscale ping 100.x.x.x` 测连通性（在电脑上执行，把 IP 换成手机的）
4. 服务是否在跑：电脑上访问 `http://127.0.0.1:8899` 能否打开

**能 ping 通但浏览器打不开**

多半是服务没在跑，或者 `config.json` 里 `server.host` 不是 `0.0.0.0`。

**速度慢**

Tailscale 优先走直连（P2P），打洞失败时会走中继（DERP）。用 `tailscale ping` 看是否走了中继。
国内环境下中继节点可能在境外，速度会受影响。如果长期很慢，可以考虑：

- **ZeroTier**：机制类似，国内可自建 Moon 中转节点，速度更可控
- **Cloudflare Tunnel**：`cloudflared tunnel --url http://localhost:8899`，好处是手机不用装 App，浏览器直接访问；但临时域名每次重启都会变，固定域名需要自有域名

**这台电脑不方便一直开**

如果家里有 NAS、树莓派、闲置的迷你主机，把 `ai-hub` 拷过去跑是最省心的 —— 功耗低、可以 7×24 开机，公网暴露风险依然为零。

---

## 七、安全提醒

- 暴露在外的第一道防线就是**访问密码**，默认是 `hub-8899`，**请务必改掉**
  （改 `config.json` 里的 `auth.password`，重启生效）
- 别把这个地址发给别人，也别贴到公开群里
- Tailscale 只管"谁能连到"，不管"连到后能干什么" —— 后者靠密码
- 建议定期看看 `config.json` 里三个 API 端点的额度消耗，发现异常及时换密钥
