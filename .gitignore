# 密钥配置 —— 绝对不要提交
config.json

# 运行时产物
models.cache.json
server.log

# 测试产物（浏览器临时 profile 用通配，避免每次新增一个就漏一个）
.edge-*/
shots/
ui.log
scan.log

# 依赖（本项目零依赖，以防万一）
node_modules/
npm-debug.log*

# 系统
.DS_Store
Thumbs.db
